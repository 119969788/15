# 最终修复方案 - ERR_PACKAGE_PATH_NOT_EXPORTED

## 问题根源

`@catalyst-team/poly-sdk` 包的 `package.json` 中没有正确配置 `exports` 字段，导致在 ESM 模式下无法通过包名加载。

## 解决方案

代码已更新为使用**直接文件路径**加载包，绕过 `exports` 限制。

### 修复内容

1. **使用绝对路径加载包**
   - 直接读取包的 `package.json` 获取 `main` 字段
   - 尝试多个可能的入口文件路径
   - 支持多种导出方式（default, PolySDK, 整个模块）

2. **多重回退机制**
   - 方法1: 使用包的绝对路径 + main 字段
   - 方法2: 尝试常见入口文件（index.js, dist/index.js 等）
   - 方法3: 最后尝试使用包名（可能会失败）

## 在服务器上应用修复

```bash
# 1. 拉取最新代码
cd /root/15
git pull

# 2. 确保依赖已安装
npm install

# 3. 测试运行
npm run dev
```

## 如果仍然失败

### 步骤1: 检查包结构

```bash
# 运行检查脚本
bash scripts/check-package-structure.sh

# 或手动检查
cat node_modules/@catalyst-team/poly-sdk/package.json | grep -E '"main"|"module"|"exports"'
ls -la node_modules/@catalyst-team/poly-sdk/
```

### 步骤2: 根据检查结果修复

如果包确实存在但路径不对，可以：

**选项A: 修改代码使用正确的路径**

编辑 `src/index.ts`，在 `possibleEntries` 数组中添加实际存在的文件路径。

**选项B: 重新安装包**

```bash
npm uninstall @catalyst-team/poly-sdk
rm -rf node_modules/@catalyst-team
npm cache clean --force
npm install @catalyst-team/poly-sdk@latest
```

**选项C: 使用包的特定版本**

```bash
npm install @catalyst-team/poly-sdk@0.2.0  # 尝试旧版本
```

### 步骤3: 临时解决方案 - 修改 package.json

如果以上都不行，可以临时移除 `"type": "module"`：

```json
{
  "type": "commonjs",  // 改为 commonjs
  // ... 其他配置
}
```

然后修改代码使用 CommonJS 语法（不推荐，需要大量修改）。

## 验证修复

运行程序后，应该看到：

```
✓ 成功加载 SDK (使用: dist/index.js)
🚀 15分钟市场套利程序启动
...
```

如果看到这个输出，说明修复成功！

## 备选方案

如果 `@catalyst-team/poly-sdk` 确实无法使用，可以考虑：

1. **使用官方 Polymarket SDK**
   ```bash
   npm install @polymarket/clob-client
   ```

2. **联系包维护者**
   - 在 GitHub 上提交 issue
   - 报告 `exports` 字段缺失的问题

3. **Fork 并修复**
   - Fork `@catalyst-team/poly-sdk` 仓库
   - 添加正确的 `exports` 配置
   - 使用修复后的版本
